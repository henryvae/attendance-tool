# 考勤管理工具 · UI 设计方案

> 版本：v104 界面升级方案 ｜ 设计风格：现代简约 ｜ 范围：不改变任何现有功能

---

## 一、设计目标

| 目标 | 说明 |
|---|---|
| 美观 | 建立统一设计系统（色彩/字体/间距/圆角/阴影），告别旧版绿色渐变风格 |
| 易用 | 信息分层清晰，导航明确，高频操作（刷新/导出/设置）一键可达 |
| 一致 | 全部组件共用一套设计令牌，视觉风格完全统一 |
| 保留功能 | 登录、本月/上月切换、日期选择、加班设置、下班提醒、迟到早退统计、刷新倒计时、导出 CSV、上下班配置、大小周逻辑 —— 全部保留 |

---

## 二、设计系统

### 2.1 色彩系统

**品牌主色（靛蓝）**
| 令牌 | 色值 | 用途 |
|---|---|---|
| Primary | `#4F6BF6` | 主按钮、选中态、高亮值 |
| Primary-600 | `#4358E0` | 主按钮 hover |
| Primary-100 | `#EDF0FE` | 选中背景、浅色强调 |
| Gradient | `#6E85FF → #4F6BF6` | Logo、头像、强调卡 |

**中性色**
| 令牌 | 色值 | 用途 |
|---|---|---|
| Ink-900 | `#1A1D26` | 主文字、数值 |
| Ink-700 | `#3A4050` | 次级文字 |
| Ink-500 | `#6B7280` | 辅助文字、标签 |
| Ink-400 | `#9CA3AF` | 占位、禁用 |
| BG | `#F6F7FB` | 应用背景 |
| Surface | `#FFFFFF` | 卡片 |
| Border | `#E8EAF0` / `#D6DAE4` | 边框 |

**语义色**
| 语义 | 色值 | 浅色底 |
|---|---|---|
| 成功 | `#16A34A` | `#E8F7EE` |
| 警告 | `#D97706` | `#FEF3E2` |
| 危险 | `#DC2626` | `#FDEBEB` |
| 信息 | `#0E7490` | `#E0F2FE` |

**可访问性**：正文对比度 ≥ 4.5:1（WCAG AA），大字/粗体 ≥ 3:1。

### 2.2 排版系统

| 层级 | 字号/字重 | 用途 |
|---|---|---|
| Display | 26px / 800 | 指标卡大数值 |
| Title | 17px / 800 | 弹窗标题 |
| Heading | 14px / 700 | 卡片标题 |
| Body | 13px / 400–600 | 正文、列表 |
| Caption | 12px / 500 | 辅助说明 |
| Mono | 等宽字体 | 时间、时长等数字 |

字体族：`Microsoft YaHei UI / Segoe UI / PingFang SC`；数字用等宽 `JetBrains Mono / Consolas` 保证对齐。

### 2.3 间距 / 圆角 / 阴影

- **间距网格**：8pt（4/8/12/16/20/24/32/40）
- **圆角**：小 6px（徽章/进度）· 中 10px（输入/按钮）· 大 16px（卡片/弹窗）· 全 999px（徽章/头像）
- **阴影**：`sm 0 1px 2px` · `md 0 4px 12px` · `lg 0 12px 32px`（卡片悬停、弹窗）

---

## 三、界面结构

### 3.1 登录界面（460×560 → 建议 420×560）

- 柔和渐变背景 + 光斑装饰
- 居中白色圆角卡片（大阴影）
- Logo 渐变方块 + 标题「欢迎回来」
- 账号 / 密码输入框（focus 显示主色光环）
- 记住账号 + 记住密码、忘记密码
- 主按钮「登录」（悬停上浮微动效）
- 细进度条 + 错误提示条

### 3.2 主界面（780×420 → 建议 960×640）

采用 **顶栏 + 左侧导航 + 内容区** 三段式：

```
┌────────────────────────────────────────────┐
│ Logo 考勤管理 v104    张伟  ⚙  ↓  ⏻      │  ← 顶栏
├──────┬─────────────────────────────────────┤
│ 今日概览│  控制行：本月/上月 日期 加班设置 刷新 │
│ 考勤记录│  ┌────┬────┬────┬────┐           │
│ 加班统计│  │应下班│工作时长│加班│累计加班│      │  ← 指标卡
│       │  └────┴────┴────┴────┘           │
│ 系统设置│  ┌────────────────┬──────────┐  │
│       │  │ 今日考勤详情     │ 打卡时间线 │  │  ← 双栏
│       │  │ 打卡记录        │ 提醒设置   │  │
│ 退出登录│  └────────────────┴──────────┘  │
└──────┴─────────────────────────────────────┘
```

**顶栏**：Logo + 应用名 + 版本徽章；右侧用户头像/姓名 + 设置/导出/退出图标按钮。

**侧边导航**：今日概览 / 考勤记录 / 加班统计 / 系统设置 / 退出登录，选中态为浅蓝底 + 主色字。

**内容区（按导航切换）**：
1. **今日概览** — 控制行 + 4 指标卡 + 今日详情 + 打卡时间线
2. **考勤记录** — 筛选行（本月/上月、日期）+ 明细表格（日期/星期/打卡/应下班/时长/加班/状态徽章）
3. **加班统计** — 平加/周加/假加/合计 4 张小卡 + 每日加班明细表
4. **系统设置** — 上下班 6 项时间表单 + 保存/取消

### 3.3 弹窗组件

- **配置弹窗**：双列表单布局，主色保存按钮 + 幽灵取消按钮
- **下班提醒弹窗**：居中卡片，图标 + 标题 + 应下班时间（等宽主色）+ 自动关闭倒计时 + 主按钮

---

## 四、组件规范

| 组件 | 规范 |
|---|---|
| 主按钮 | 高 40px、主色底白字、圆角 10px、hover 变深上浮 |
| 次按钮 | 白底、边框、hover 主色描边 |
| 输入框 | 高 38–44px、浅底、focus 主色光环（4px 浅色 ring） |
| 分段控件 | 本月/上月，选中项白底 + 主色字 |
| 徽章 | 圆角全、语义浅底 + 深色字：正常(绿)/迟到(红)/加班(橙)/休息(灰)/小周(蓝) |
| 表格 | 表头浅灰底、行 hover 浅蓝、斑马纹 |
| 指标卡 | 白底 + 等宽大数字；主卡用渐变强调 |

---

## 五、交付物

| 文件 | 说明 |
|---|---|
| `UI设计方案.html` | 高保真交互设计稿，浏览器直接打开预览（含全部界面） |
| `theme_modern.qss` | PyQt5 可落地样式表，设计令牌完整实现 |

---

## 六、落地建议（不改变功能）

1. 将 `theme_modern.qss` 内容合并进 `attendance_tool.py` 的 `THEME` / 全局 QSS
2. 主窗口改为「顶栏 + 左侧导航 + 内容区」结构，原左侧控制面板拆分为：控制行（概览页）+ 导航项
3. 统计卡沿用 `_update_stats` 数据，仅更换容器为卡片样式
4. 保留全部信号/槽逻辑，仅调整布局容器与样式
